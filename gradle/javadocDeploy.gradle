task javadocDeploy(type: Copy) {
    dependsOn javadoc
    from project.javadoc.destinationDir
    into "${rootProject.buildDir}/javadoc/${version}/${(project.name == "docs") ? "all" : project.name}"

    includeEmptyDirs = true
}